#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;

    int A [n];

    double result=0.0,sum=0.0;

    for(int i=0; i<n; i++)
    {
        cin>>A[i];
        sum+=A[i];
    }
      result =sum/n;

      cout<<fixed<<setprecision(12)<<result<<endl;

    return 0;
}
