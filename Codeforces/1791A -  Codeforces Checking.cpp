#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    while (n--) {
        char ch;
        cin >> ch;

    if (ch == 'c' || ch == 'o' || ch == 'd' || ch == 'e' || ch == 'f' || ch == 'o' || ch == 'r' || ch == 'c' || ch == 'e' || ch == 's')
     {
         cout<<"Yes"<<endl;
     }
     else
     {

         cout<<"No"<<endl;
     }


    }

    return 0;
}
