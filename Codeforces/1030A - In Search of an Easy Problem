#include <iostream>
using namespace std;

int main()
{
    int n;
    cin>>n;

    int A[n];

    string result = "easy";

    for (int i=1; i<=n; i++)
    {
        cin>> A[i];
        if (A[i] == 1)
        {
            result = "hard";
        }
    }

    cout<< result<<endl;

    return 0;
}
